# 轻量桌面重构执行清单

更新：2026-09-05。本文是后续工作的唯一进度清单；其他历史计划不作为完成依据。

目标：减少安装体积和日常写盘，收敛重复实现，保留文档编辑、AI 写作、资料引用和导出。已移除 Playwright，已完成的局部缓存限额不重复计为本轮成果。

执行规则：按下面顺序实施；每次改动补相应验证，验证后才勾选。不得整体删除 `.data`、密钥或用户文档。保留已有未提交修改。失败不隐式切换模型、不扩大编辑范围。未跑 GUI/真实模型/打包的项目不得标记验收通过。

## P0：入口与可控启动

- [x] A1 统一产品入口为桌面；浏览器服务仅保留显式开发入口。默认 CLI、双击入口和快速开始一致；缺少桌面依赖时清楚报错，不自动退回浏览器。已通过入口契约测试，真实窗口验收见 D2。
- [x] A2 启动入口不检查模型、不启动 Ollama、不下载模型。已移除两处启动预热代码并通过禁止隐式下载的回归；未配置 API 时真实窗口能否完整使用仍由 D2 验收，任务内部迁移见 C6。
- [x] A3 修正桌面无限 reload、阻塞等待及服务退出生命周期。服务启动等待限 30 秒、非阻塞定时检查、失败不自动刷新、退出请求停止并最多等待 5 秒；已通过替身契约测试，真实 GUI 见 D1/D2。
- [ ] A4 修正桌面下载接口，禁用无用插件；不默认关闭浏览器沙箱。验收：桌面 API 契约测试及实际导出验证。
- [ ] A5 完成桌面容器最终取舍。当前复用 PySide + Svelte，不新增第二套桌面实现；测量 QtWebEngine 体积后决定是否迁移系统 WebView，不能将测试浏览器删除等同于去除内嵌内核。
- [ ] A6 退役 Tauri 实验链路与相关残留；先核对已有 7 个未提交删除，避免覆盖用户工作。

## P1：存储和空间

- [x] B1 提供只读空间盘点：`python -B -m writing_agent.storage_report`。区分受保护数据、工具缓存、依赖、构建产物，输出逻辑大小和读取错误，不读取内容、不跟随重解析点；`.data` 内的细分归属留待 B4，不冒充可删除缓存。
- [ ] B2 统一日志、诊断、临时文件的容量和保留期限；验证连续运行后增长有界。
- [ ] B3 统一旧文本块/检查点生命周期，识别跨文档引用；只清理确定无引用、过期的可重建数据，提供预览。
- [ ] B4 明确缓存目录和用户数据目录；处理旧位置迁移、失败恢复、多进程并发，不仅限制单一缓存。
- [ ] B5 完成 C 盘旧 Playwright 缓存人工清理确认（此前约 1.31 GiB，工具删除被策略拦截）；不绕过删除限制，不清理其他应用共享资源。

## P2：核心工作流与实现取舍

- [ ] C1 修订工作流拆分输入与依赖，移除直接应用对象依赖，保留作用域约束与质量门禁。
- [ ] C2 局部编辑、图表工作流移除 Web/响应类型依赖，HTTP/SSE 在入口层适配。
- [ ] C3 工作区、版本、引用等服务不再反向访问 `app_v2` 全局对象。
- [ ] C4 独立任务状态、错误类型、取消、超时及重试预算；取消后关闭流和资源，不继续写文档。
- [ ] C5 盘点 Python/Rust、单/双生成引擎、Node 网关及 fallback 调用者，逐项保留唯一必要实现；删除前有等价回归证据。
- [ ] C6 模型选择贯通生成、修订、局部编辑和检索；DeepSeek/自定义 API 不再被旧 Ollama 检查阻挡。验证失败不偷偷换服务、不下载本地模型。
- [ ] C7 收敛静态模型列表和配置；密钥存储安全化（优先系统凭据库），支持旧配置迁移、删除与错误提示，不打印密钥。

## P3：编辑器与验证

- [ ] D1 建立不依赖 Playwright 的桌面测试：启动、失败、关闭、菜单与下载。
- [ ] D2 验收新建→生成→局部修改→保存→关闭重开→导出完整流程，明确真实模型测试成本。
- [ ] D3 验收长文档、表格、图片、撤销/重做、版本恢复，失败不得覆盖原文。
- [ ] D4 验收分页、DOCX 格式一致性及 PDF 能力边界；列明与 Word 的差距，不以 API 测试替代排版验收。
- [ ] D5 增加前端独立类型/组件检查，保留导出与核心回归；测试产物集中到临时目录。
- [ ] D6 完成全量回归，并记录测试范围、失败和未覆盖项。

## P4：打包和交付

- [ ] E1 产出单一桌面安装包；普通使用不需要 Node/Rust/开发工具，不能只交付启动脚本。
- [ ] E2 测量安装包/安装后大小、冷启动时间、空闲内存、重复任务磁盘增量；据测量制定并执行上限。
- [ ] E3 精简文档为使用说明、必要开发说明和本清单；移除失效 Web 产品/部署说明，保留 Git 可恢复历史。
- [ ] E4 完成内容与实现清单、已知问题、恢复方式，并核对主分支提交与工作区差异。

## 执行记录

- 2026-09-05：建立清单。开始 P0。新发现：桌面启动仍可能自动拉模型；界面失败存在无限刷新；当前 Qt 下载代码使用旧接口。优先修正这些可导致额外资源消耗或阻塞的路径。
- 第一批：A1/A2/A3/B1 已完成代码及无 GUI 契约验证。A4 的 Qt6 下载接口、禁用插件和恢复默认沙箱代码已修改，实际桌面导出未验收，保持未勾选。D1 已建立启动/失败/下载替身测试，但实际菜单和窗口交互尚未验证。
- 空间基线：只读盘点约 379 MiB；依赖约 278 MiB，工具缓存约 24 MiB，受保护 `.data` 约 9.6 MiB。不是安装包大小。旧 `.venv-audit` 约 127 MiB，基础解释器不可用，未删除。
- 自动续做：当前任务已设置每 30 分钟继续一次；每轮更新本清单，只提交验证过的本轮改动。完成或全部剩余事项需用户决定时停止。电脑及应用需保持运行；不要再建立重复自动任务。
- 下一轮先完成 A4/D1 可执行验证，再推进 B2/B4；C6 特别注意修订流程仍直接调用 Ollama，不能宣称模型接入已经全部统一。
- 继续检查已移除后端 lifespan 的隐式 Ollama 预热（第三处启动路径）。验证：15 项桌面/入口契约测试、5 项只读盘点测试；未安装任何新依赖。PowerShell 脚本试运行被本机执行策略拒绝，未绕过策略；真实 GUI、完整后端回归和安装包未验收。
