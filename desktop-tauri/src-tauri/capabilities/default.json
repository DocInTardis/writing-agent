{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No privileged frontend APIs are required; the UI talks only to its local Python service.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
